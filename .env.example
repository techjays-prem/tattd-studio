# Tattd Studio — environment template
# Copy to `.env` and fill in. `.env` is gitignored.

# Gemini Embedding 2 + Generation Client (Nano Banana 2 / Pro)
GEMINI_API_KEY=

# Vertex AI project for Gemini Embedding 2 (production-grade auth path)
VERTEX_PROJECT_ID=

# Replicate (LoRA Artifact training via ai-toolkit)
REPLICATE_API_TOKEN=

# Hugging Face (FLUX.2-dev / FLUX.2-klein base weights for the Comparison Matrix)
HF_TOKEN=
